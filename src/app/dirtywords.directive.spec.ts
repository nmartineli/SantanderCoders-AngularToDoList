import { DirtywordsDirective } from './dirtywords.directive';

describe('DirtywordsDirective', () => {
  it('should create an instance', () => {
    const directive = new DirtywordsDirective();
    expect(directive).toBeTruthy();
  });
});
